#ifndef IO_H
#define IO_H

#include "mzm/types.h"

#define write32(addr, val) (0)
#define write16(addr, val) (0)
#define write8(addr, val) (0)

#define read32(addr) (0)
#define read16(addr) (0)
#define read8(addr) (0)

#endif /* IO_H */